{
  "name": "mdxg",
  "private": true,
  "description": "Markdown Experience Guidelines (MDXG) — a specification for how interfaces should present and interact with markdown documents.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/vercel-labs/mdxg"
  },
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2"
  }
}
